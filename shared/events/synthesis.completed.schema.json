{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://eventforge.dev/schemas/synthesis.completed.schema.json",
  "title": "SynthesisCompletedPayload",
  "description": "Payload for eventforge.synthesis.completed — mock markdown report stored for the job.",
  "type": "object",
  "required": ["report_id", "note_count"],
  "properties": {
    "report_id": {
      "type": "string",
      "format": "uuid",
      "description": "Primary key of the synthesis_reports row written during synthesis."
    },
    "note_count": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of research notes merged into the report."
    }
  },
  "additionalProperties": false
}
