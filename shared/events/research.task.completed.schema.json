{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://eventforge.dev/schemas/research.task.completed.schema.json",
  "title": "ResearchTaskCompletedPayload",
  "description": "Payload for eventforge.research.task.completed — mock research note persisted for a sub-task.",
  "type": "object",
  "required": ["task_id", "note_id", "task_index"],
  "properties": {
    "task_id": {
      "type": "string",
      "format": "uuid",
      "description": "Stable identifier of the completed research sub-task."
    },
    "note_id": {
      "type": "string",
      "format": "uuid",
      "description": "Primary key of the research_notes row written during this sub-task."
    },
    "task_index": {
      "type": "integer",
      "minimum": 0,
      "description": "Zero-based index matching the dispatched sub-task."
    }
  },
  "additionalProperties": false
}
