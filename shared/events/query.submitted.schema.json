{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://eventforge.dev/schemas/query.submitted.schema.json",
  "title": "QuerySubmittedPayload",
  "description": "Payload for eventforge.query.submitted — the research request that starts the pipeline.",
  "type": "object",
  "required": ["topic"],
  "properties": {
    "topic": {
      "type": "string",
      "minLength": 1,
      "description": "Research question or topic text from the user."
    },
    "depth": {
      "type": "string",
      "enum": ["quick", "standard", "deep"],
      "default": "standard",
      "description": "How thorough the pipeline should be; maps to jobs.depth."
    },
    "max_sources": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "description": "Optional cap on sources fetched during ingestion."
    }
  },
  "additionalProperties": false
}
